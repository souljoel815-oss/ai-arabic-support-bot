# Spec Kit local-only settings
.claude/settings.local.json

# Feature 001-ai-support-agent — explicit per tasks.md T002
agent/deploy/.env
agent/eval/results/*.jsonl
agent/eval/.venv/

# Python (eval harness)
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.tmp
.vscode/
.idea/

# Feature 003-crypto-trading-bot — local data dir and Streamlit secrets
.crypto-trading-bot/
.streamlit/secrets.toml

# Python build / dist (added for trading_bot pyproject.toml)
build/
dist/
*.egg
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*

# Feature 008-egypt-tax-accounting — .NET / C# build artifacts
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
*.user
*.suo
*.userosscache
*.sln.docstates
*.userprefs
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*
*.VisualState.xml
TestResult.xml
*.coverage
*.coveragexml
.vs/
project.lock.json
project.fragment.lock.json
artifacts/

# NuGet
*.nupkg
*.snupkg
.nuget/
packages/
!packages/build/
!packages/repositories.config

# JetBrains Rider
.idea/

# 008-egypt-tax-accounting — local dev/state outputs (per quickstart §3 + FR-028)
EgyptTax-Dev/
appsettings.Development.json
appsettings.Local.json
**/serilog.user.json

# 008-egypt-tax-accounting — Playwright + E2E
playwright-report/
playwright/.cache/
test-results/

# Microsoft.Playwright auto-installed browsers
**/bin/Debug/net*/playwright/
**/bin/Release/net*/playwright/

# WiX installer (T247) build outputs
**/*.wixobj
**/*.wixpdb
**/*.msi
**/*.wixproj.user

# Licensing — vendor PRIVATE signing key. NEVER commit. Loss of
# this file = inability to issue new licenses (recoverable via
# keypair rotation). Compromise = pirates can issue valid
# licenses (emergency keypair rotation required).
vendor-keys.json
**/vendor-keys.json

# Per-customer licensing artefacts (machine-bound, never in source).
**/license.token
**/license.activated
**/share*.bin
**/share*.mask

# Issue-License.ps1 output — per-customer signed tokens + README.
licenses/

# Build artefacts (dotnet publish output) — never source-tree.
publish/

# Foreign Python project unrelated to DaftarX that landed in
# src/ by accident (see DIAGNOSTIC-REPORT.md Finding 10). Listed
# here so future `git status` is clean; move them out of the tree
# when convenient.
src/trading_bot/
src/trading_bot.egg-info/
src/003-crypto-trading-bot/
src/src.zip
specs/003-crypto-trading-bot/
specs/003-crypto-trading-bot.zip
specs/005-freelancer-billing-platform/
specs/006-pro-trader-context/
specs/007-fpl-analyzer/
specs/007-fpl-analyzer.rar
specs/fantasy/
alembic.ini
pyproject.toml
/migrations/
tests/__init__.py
tests/contract/
tests/integration/
tests/unit/

# Local scratch
scratch/

# Agent / spec-kit scaffolding (per-machine, not source-of-truth).
.claude/
.specify/

# Local dev-server boot logs (per-session, never committed).
boot.log
boot-err.log

# Roslyn / dotnet build cache (per-build, regenerates).
*.csproj.lscache

# UI-handoff working artefacts (designer drops the zip; we extract
# locally; the handoff zip itself is regenerated on demand from
# the canonical files in src/).
.tmp-handoff-v2/
daftarx-ui-blazor-handoff-v2/
daftarx-ui-blazor-handoff-v2.zip
ui-handoff-2026-05-15.zip
