@*
    Gux.13 Tab 5 — Email Settings placeholder.
    Real implementation lands in Phase 2g (MAPI + SMTP).
*@
<div class="admin-tab-placeholder">
    <h2>@(IsArabic ? "البريد الإلكتروني" : "Email Settings")</h2>
    <p class="page-subtitle">
        @(IsArabic
            ? "هذا التبويب قيد التطوير. سيتضمن: طريقة الإرسال (MAPI أو SMTP مباشر)، إعدادات Gmail/Outlook/Yahoo، اختبار الإرسال."
            : "This tab is under construction. Will include: send method (MAPI or direct SMTP), Gmail/Outlook/Yahoo presets, test send.")
    </p>
</div>
