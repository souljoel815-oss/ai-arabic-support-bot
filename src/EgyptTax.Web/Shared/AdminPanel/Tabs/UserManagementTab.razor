@*
    Gux.13 Tab 6 — User Management placeholder.
    Real implementation lands in Phase 2i (after role enforcement infra).
*@
<div class="admin-tab-placeholder">
    <h2>@(IsArabic ? "إدارة المستخدمين" : "User Management")</h2>
    <p class="page-subtitle">
        @(IsArabic
            ? "هذا التبويب قيد التطوير. سيتضمن: قائمة المستخدمين، الأدوار (Admin/Accountant/Cashier/View-only)، إعادة ضبط كلمة المرور."
            : "This tab is under construction. Will include: user list, roles (Admin/Accountant/Cashier/View-only), password reset.")
    </p>
</div>
