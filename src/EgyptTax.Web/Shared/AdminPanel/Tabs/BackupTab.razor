@*
    Gux.13 Tab 8 — Backup placeholder.
    Real implementation lands in Phase 2j (provider-aware backup engine).
*@
<div class="admin-tab-placeholder">
    <h2>@(IsArabic ? "النسخ الاحتياطي" : "Backup")</h2>
    <p class="page-subtitle">
        @(IsArabic
            ? "هذا التبويب قيد التطوير. سيتضمن: نسخ تلقائي/يدوي، استعادة، احتفاظ بـ30 نسخة افتراضياً، يدعم SQL Server و SQLite."
            : "This tab is under construction. Will include: auto/manual backup, restore, 30-backup retention default, supports both SQL Server and SQLite.")
    </p>
</div>
