@page "/settings/company"
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Policy = "FullyAuthenticated")]

@*
    Gux.13 — direct-link host for the Company Profile body.
    The real UI lives in <CompanyProfilePanel /> so the new
    /settings?tab=company-profile and the legacy /settings/company
    bookmark mount the same component.
*@

<CompanyProfilePanel />
