"""Prediction models for the FPL analyzer.

Modules land here as the relevant tasks complete:

* ``baseline``   — naive PPG × form baseline (T026)
* ``ensemble``   — XGB + LGBM + CatBoost mean head, plus quantile and Poisson heads (T027)
"""
