# ─────────────────────────────────────────────────────────────────────────────
# FPL Ultimate Analyzer V11 — dependencies (Python 3.14 compatible)
#
# Install:   pip install -r requirements-all.txt
# ─────────────────────────────────────────────────────────────────────────────

# Core data + numerics
numpy>=1.24
pandas>=2.0
scipy>=1.10

# HTTP
requests>=2.28

# ML ensemble
xgboost>=2.0
lightgbm>=4.0
scikit-learn>=1.3

# Linear-programming squad optimizer
PuLP>=2.7

# UI
streamlit>=1.28
plotly>=5.17

# ─── Optional: extra model in the ensemble ─────────────────────────────────
catboost>=1.2

# ─── Optional: V11 fuzzy team-name matching ────────────────────────────────
rapidfuzz>=3.5

# ─── Understat (xG/xA features) — DROPPED ──────────────────────────────────
# The `understat` package on PyPI hard-pins aiohttp==3.8.3, which has no
# wheel for Python 3.14. The library itself is unmaintained (last release
# 2022), so we can't easily work around it.
#
# Run the analyzer with the "Skip Understat data" toggle in the Streamlit
# sidebar (or --no-understat on the CLI). All other features still work;
# you only lose ~5-8% MAE accuracy from missing xG/xA inputs.
#
# If you really need Understat: downgrade to Python 3.11 or 3.12 in a
# separate venv, then `pip install understat aiohttp` will work.